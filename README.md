# Video_analysis_and_validation_agent
This is part of assessment for agent validation and initiaion..

## Development
- `uv sync` - create/update the `.venv` and install dependencies.
- `uv add <package>` - add a runtime dependency.
- `uv add --dev <package>` - add a development dependency.
- `uv run python -m <package>` - run an entry point once it exists.
