[project]
name = "video-analysis-and-validation-agent"
version = "0.1.0"
description = "Video analysis and validation agent."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = []
